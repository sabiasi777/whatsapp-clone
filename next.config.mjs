/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {hostname: 'fastidious-ibex-667.convex.cloud'}
        ]
    }
};

export default nextConfig;
